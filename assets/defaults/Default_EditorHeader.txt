#pragma once

#include <Engine.h>

using namespace Engine;

class {FileName} final : public EditorInterface
{
public:
    // Creating the editor window
    {FileName}() : EditorInterface(L"{FileName}") {}

    // Draw the window
    void DrawInterface() override;
};